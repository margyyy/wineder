---
phase: 04-winery-and-workshop-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - src/lib/domain/verification/verifiedWine.ts
  - src/lib/domain/verification/verifiedWine.test.ts
  - src/lib/data/repositories/managementRepository.ts
autonomous: true
requirements:
  - MGMT-05
  - MGMT-06
  - VERI-01
  - WINE-04
  - WINE-05
must_haves:
  truths:
    - "Winery-managed production and additive data can be persisted per wine"
    - "Verified status is deterministically derivable from wine data completeness"
    - "Management repository exposes a single contract for phase-4 routes/pages"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Workshop/business entities plus wine management fields"
    - path: "src/lib/domain/verification/verifiedWine.ts"
      provides: "pure function for verified badge rule"
    - path: "src/lib/data/repositories/managementRepository.ts"
      provides: "CRUD entrypoints for workshop/winery management"
  key_links:
    - from: "src/lib/domain/verification/verifiedWine.ts"
      to: "src/lib/data/repositories/managementRepository.ts"
      via: "verification field update"
      pattern: "isWineVerified"
    - from: "prisma/schema.prisma"
      to: "src/lib/data/repositories/managementRepository.ts"
      via: "prisma model usage"
      pattern: "prisma\.(workshop|wine|wineAdditive)"
---

<objective>
Establish the persistence and domain foundation for workshop/business management and verification logic.

Purpose: All phase-4 routes and pages depend on consistent schema + repository contracts.
Output: schema updates, verification domain function, management repository methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@prisma/schema.prisma
@prisma/seed.ts
@src/lib/data/repositories/wineRepository.ts
@src/lib/data/repositories/additiveRepository.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schema for workshop entities and wine management metadata</name>
  <files>prisma/schema.prisma, prisma/seed.ts</files>
  <behavior>
    - Workshop records can be created with coordinates and category.
    - Wine has optional production description and additive relations usable by management pages.
    - Existing discovery and matching tables remain compatible.
  </behavior>
  <action>Add a `Workshop` model (category: restaurant/bar/club/winery, name, slug, lat, lng, profile text, history text, optional linked winery). Add wine fields required by phase 4: `productionDescription` (optional) and `isVerified` default false. Ensure additive relation remains reusable for winery-managed assignment. Update seed with at least one non-winery workshop sample and additive examples linked to wines.</action>
  <verify>
    <automated>npx prisma format && npx prisma validate && npx prisma db push && npx prisma db seed</automated>
  </verify>
  <done>Schema and seed support management and verification workflows without breaking existing phase data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement verification domain rule as pure function</name>
  <files>src/lib/domain/verification/verifiedWine.ts, src/lib/domain/verification/verifiedWine.test.ts</files>
  <behavior>
    - Wine is verified only when production description exists and at least one additive exists.
    - Missing either condition yields non-verified result.
  </behavior>
  <action>Create `isWineVerified({ productionDescription, additiveCount })` with strict boolean outcome per VERI-01 and unit tests covering positive and negative combinations.</action>
  <verify>
    <automated>npm run test -- src/lib/domain/verification/verifiedWine.test.ts || npx vitest run src/lib/domain/verification/verifiedWine.test.ts</automated>
  </verify>
  <done>Verification logic is centralized and test-backed for consistent badge behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Build management repository contract for phase-4 routes/pages</name>
  <files>src/lib/data/repositories/managementRepository.ts</files>
  <action>Create repository methods for: workshop create/list, winery/workshop profile update, wine attach to entity, wine production description update, wine additive assignment, and verified flag refresh using `isWineVerified`. Keep method signatures explicit and return DTOs ready for route handlers.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Phase-4 APIs/pages can rely on one repository layer instead of duplicated Prisma logic.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                 |
| ------------------------------------ | ----------------------------------------------------------- |
| management payload -> repository     | untrusted admin-like input mutates catalog and profile data |
| verification rule -> persisted badge | derived trust signal is exposed to end users                |

## STRIDE Threat Register

| Threat ID | Category | Component                     | Disposition | Mitigation Plan                                                         |
| --------- | -------- | ----------------------------- | ----------- | ----------------------------------------------------------------------- |
| T-04-01   | T        | workshop/profile write paths  | mitigate    | validate category allowlist and required coordinates before persistence |
| T-04-02   | R        | wine verification updates     | mitigate    | recompute `isVerified` from deterministic rule on each relevant update  |
| T-04-03   | E        | management repository methods | accept      | MVP has no auth tiers yet; scope accepted and documented for phase 4    |

</threat_model>

<verification>
- `npx prisma validate`
- `npx prisma db push`
- `npx prisma db seed`
- `npm run test -- src/lib/domain/verification/verifiedWine.test.ts`
- `npm run build`
</verification>

<success_criteria>

- MGMT-05, MGMT-06, VERI-01 foundations are complete.
- Data layer supports WINE-04 and WINE-05 rendering requirements.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-winery-and-workshop-management/04-winery-and-workshop-management-01-SUMMARY.md`
</output>
