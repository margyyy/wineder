---
phase: 04-winery-and-workshop-management
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-03-PLAN.md
files_modified:
  - src/app/(public)/wine/[slug]/page.tsx
  - src/components/wine/WineVerifiedBadge.tsx
  - src/components/wine/WineAdditiveChips.tsx
  - src/components/wine/AdditiveModal.tsx
  - src/app/(public)/cantina/[slug]/page.tsx
autonomous: true
requirements:
  - MGMT-07
  - VERI-01
  - VERI-02
  - VERI-03
  - WINE-04
  - WINE-05
must_haves:
  truths:
    - "Verified badge appears only when verification rule passes"
    - "Wine detail shows production description when present"
    - "Additives render as clickable boxes opening modal with DB description"
    - "Public winery profile page is available for winery category"
  artifacts:
    - path: "src/components/wine/WineVerifiedBadge.tsx"
      provides: "explicit verified/hidden badge rendering"
    - path: "src/components/wine/WineAdditiveChips.tsx"
      provides: "clickable additive boxes"
    - path: "src/app/(public)/cantina/[slug]/page.tsx"
      provides: "public winery profile route"
  key_links:
    - from: "src/app/(public)/wine/[slug]/page.tsx"
      to: "src/components/wine/WineVerifiedBadge.tsx"
      via: "badge rendering"
      pattern: "WineVerifiedBadge"
    - from: "src/components/wine/WineAdditiveChips.tsx"
      to: "src/components/wine/AdditiveModal.tsx"
      via: "modal open state"
      pattern: "AdditiveModal"
---

<objective>
Complete public-facing transparency and winery profile features for phase 4.

Purpose: Phase-4 outcome requires visible verification and additive transparency on wine detail.
Output: verified badge UI logic, additive modal flow, winery public profile page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/app/(public)/wine/[slug]/page.tsx
@src/lib/data/repositories/managementRepository.ts
@src/lib/data/repositories/wineryRepository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable verified badge and additive modal components</name>
  <files>src/components/wine/WineVerifiedBadge.tsx, src/components/wine/WineAdditiveChips.tsx, src/components/wine/AdditiveModal.tsx</files>
  <action>Create components for verification and additives display: badge visible only when `isVerified` true; additive chips open modal showing additive description from DB payload.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>VERI-02, VERI-03, WINE-05 component-level behavior is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Extend wine detail page for production description, badge, and additives</name>
  <files>src/app/(public)/wine/[slug]/page.tsx</files>
  <action>Update wine detail page to render optional production description section (WINE-04), verified badge (VERI-02/03), and additive chip list with modal (WINE-05) using new components and management/discovery data contracts.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Wine detail transparency requirements are complete and visible.</done>
</task>

<task type="auto">
  <name>Task 3: Create public winery profile route</name>
  <files>src/app/(public)/cantina/[slug]/page.tsx</files>
  <action>Create public profile page for winery category entities only. Display profile/history text, linked wines, and key winery metadata. If entity is not winery category or missing, return not found.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MGMT-07 is complete via public winery page routing and rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted verification fields -> public badge rendering | trust indicator is exposed directly in UI |
| additive descriptions -> modal content | DB text is rendered in user-facing modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | T | verified badge rendering | mitigate | render badge only from server-derived `isVerified` field tied to rule |
| T-04-11 | I | additive modal content | accept | additive descriptions are public catalog metadata, not sensitive |
| T-04-12 | S | public cantina route | mitigate | enforce winery category check before rendering public profile |
</threat_model>

<verification>
- `npm run build`
- Manual check on wine detail: verified/non-verified wines, additive modal content.
</verification>

<success_criteria>
- MGMT-07, VERI-01..03, WINE-04, WINE-05 are complete in public-facing UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-winery-and-workshop-management/04-winery-and-workshop-management-04-SUMMARY.md`
</output>
