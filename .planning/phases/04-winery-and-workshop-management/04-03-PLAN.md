---
phase: 04-winery-and-workshop-management
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/app/(public)/add-workshop/page.tsx
  - src/app/(public)/manage-cantina/[slug]/page.tsx
  - src/app/(public)/manage-azienda/[slug]/page.tsx
  - src/components/management/WorkshopCreateForm.tsx
  - src/components/management/CantinaManager.tsx
  - src/components/management/AziendaManager.tsx
autonomous: false
requirements:
  - MGMT-01
  - MGMT-02
  - MGMT-03
  - MGMT-04
  - MGMT-05
  - MGMT-06
must_haves:
  truths:
    - "Users can open /add-workshop and submit category + coordinates"
    - "Cantina managers can edit profile/history and manage wine metadata"
    - "Azienda managers can edit profile and attach catalog wines"
  artifacts:
    - path: "src/app/(public)/add-workshop/page.tsx"
      provides: "workshop creation UI"
    - path: "src/app/(public)/manage-cantina/[slug]/page.tsx"
      provides: "cantina management UI"
    - path: "src/app/(public)/manage-azienda/[slug]/page.tsx"
      provides: "azienda management UI"
  key_links:
    - from: "src/components/management/WorkshopCreateForm.tsx"
      to: "/api/workshops"
      via: "form submission"
      pattern: "fetch\('/api/workshops'"
    - from: "src/components/management/CantinaManager.tsx"
      to: "/api/manage/cantina/"
      via: "profile/catalog updates"
      pattern: "manage/cantina"
---

<objective>
Build phase-4 management pages and forms for workshop, cantina, and azienda operations.

Purpose: requirements are user-facing; API-only completion is insufficient.
Output: management pages, reusable form components, and route wiring to management APIs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-winery-and-workshop-management/04-02-PLAN.md
@src/styles/tokens.css
@src/app/(public)/discover/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /add-workshop page and form component</name>
  <files>src/app/(public)/add-workshop/page.tsx, src/components/management/WorkshopCreateForm.tsx</files>
  <action>Create page and form for workshop creation with fields: name, slug, category, lat, lng, profile text. Submit to `/api/workshops`, show success/error feedback, and preserve mobile-first behavior.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MGMT-01 UI flow is functional and connected to API.</done>
</task>

<task type="auto">
  <name>Task 2: Create cantina management page with wine metadata controls</name>
  <files>src/app/(public)/manage-cantina/[slug]/page.tsx, src/components/management/CantinaManager.tsx</files>
  <action>Build cantina manager UI to edit profile/history text, attach wines from catalog, and edit per-wine production description/additives. Persist through `/api/manage/cantina/[slug]` and show update status states.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MGMT-02, MGMT-04, MGMT-05, MGMT-06 have complete UI interactions for cantina.</done>
</task>

<task type="auto">
  <name>Task 3: Create azienda management page for non-winery entities</name>
  <files>src/app/(public)/manage-azienda/[slug]/page.tsx, src/components/management/AziendaManager.tsx</files>
  <action>Build azienda manager UI for profile editing and wine attachment using `/api/manage/azienda/[slug]`. Keep form language and styling aligned with existing Italian UX patterns.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MGMT-03 and MGMT-04 are complete for non-winery management path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification for management flows</name>
  <action>Run manual UI pass to verify management forms are usable and payload mapping is correct for workshop/cantina/azienda screens.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Human confirms forms and submissions behave correctly or reports concrete issues.</done>
  <what-built>New management routes and form-driven write flows for phase 4 entities.</what-built>
  <how-to-verify>
    1. Open `/add-workshop` and create a workshop with valid coordinates.
    2. Open `/manage-cantina/[slug]`, update profile text, assign wines and additives.
    3. Open `/manage-azienda/[slug]`, update profile and attach wines.
    4. Confirm success feedback and persisted state on refresh.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| management forms -> management APIs | browser-entered text and coordinates mutate persistent records |
| route params -> page data loaders | slug-based lookups determine which entity is edited |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T | management form payloads | mitigate | client-side constraints + server validation before persistence |
| T-04-08 | S | slug route targeting | mitigate | fetch APIs return not-found errors for unknown slugs and block writes |
| T-04-09 | D | large form submissions | accept | MVP data volume is low; standard JSON posts are acceptable |
</threat_model>

<verification>
- `npm run build`
- Checkpoint: human verification of all management forms.
</verification>

<success_criteria>
- MGMT-01..MGMT-06 user-facing flows are complete and verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-winery-and-workshop-management/04-winery-and-workshop-management-03-SUMMARY.md`
</output>
