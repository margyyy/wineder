---
phase: 04-winery-and-workshop-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/app/api/workshops/route.ts
  - src/app/api/workshops/validators.ts
  - src/app/api/workshops/route.test.ts
  - src/app/api/manage/cantina/[slug]/route.ts
  - src/app/api/manage/azienda/[slug]/route.ts
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02
  - MGMT-03
  - MGMT-04
must_haves:
  truths:
    - "Workshops can be created with category and coordinates"
    - "Cantina and azienda management endpoints can update profiles"
    - "Management endpoints can attach wines to entities"
  artifacts:
    - path: "src/app/api/workshops/route.ts"
      provides: "workshop creation endpoint for /add-workshop"
    - path: "src/app/api/manage/cantina/[slug]/route.ts"
      provides: "cantina profile and catalog management"
    - path: "src/app/api/manage/azienda/[slug]/route.ts"
      provides: "azienda profile and catalog management"
  key_links:
    - from: "src/app/api/manage/cantina/[slug]/route.ts"
      to: "src/lib/data/repositories/managementRepository.ts"
      via: "profile + wine association writes"
      pattern: "attachWine"
    - from: "src/app/api/workshops/route.ts"
      to: "src/lib/data/repositories/managementRepository.ts"
      via: "workshop creation"
      pattern: "createWorkshop"
---

<objective>
Deliver API contracts for workshop creation and management operations.

Purpose: Phase-4 UI screens need stable server routes for writing entity/profile/catalog data.
Output: validated route handlers for workshops, cantina management, and azienda management.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-winery-and-workshop-management/04-01-PLAN.md
@src/lib/data/repositories/managementRepository.ts
@src/app/api/matching/survey/validators.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add /api/workshops creation endpoint with strict payload validation</name>
  <files>src/app/api/workshops/route.ts, src/app/api/workshops/validators.ts, src/app/api/workshops/route.test.ts</files>
  <behavior>
    - valid workshop payload persists entity with category + coordinates.
    - invalid category or missing coordinates returns 400.
  </behavior>
  <action>Create POST `/api/workshops` using repository `createWorkshop`. Validate payload fields (`name`, `slug`, `category`, `lat`, `lng`, optional profile text) and enforce category allowlist. Add tests for valid payload and validation failures.</action>
  <verify>
    <automated>npm run test -- src/app/api/workshops/route.test.ts || npx vitest run src/app/api/workshops/route.test.ts</automated>
  </verify>
  <done>MGMT-01 backend requirement is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cantina management API route</name>
  <files>src/app/api/manage/cantina/[slug]/route.ts</files>
  <action>Create route handlers for GET/PUT to read and update winery profile/history and attach catalog wines by IDs. Persist production description and additive assignments through repository helpers, then refresh verified flag for updated wines.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MGMT-02 and MGMT-04 server operations are available for cantina management pages.</done>
</task>

<task type="auto">
  <name>Task 3: Implement azienda management API route</name>
  <files>src/app/api/manage/azienda/[slug]/route.ts</files>
  <action>Create route handlers for GET/PUT to manage non-winery workshop/business profile and linked catalog wines. Reuse shared validation patterns and repository methods to keep cantina/azienda behavior aligned where applicable.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MGMT-03 and MGMT-04 server operations are available for azienda management pages.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                             |
| ------------------------------------------ | ------------------------------------------------------- |
| HTTP management requests -> route handlers | untrusted client payload reaches write endpoints        |
| route handlers -> management repository    | high-impact catalog mutations pass to persistence layer |

## STRIDE Threat Register

| Threat ID | Category | Component                      | Disposition | Mitigation Plan                                                             |
| --------- | -------- | ------------------------------ | ----------- | --------------------------------------------------------------------------- |
| T-04-04   | T        | workshop create payload        | mitigate    | validate and reject unknown categories/invalid coordinates in validators    |
| T-04-05   | S        | cantina/azienda slug targeting | mitigate    | route resolves by slug through repository and returns 404 on missing entity |
| T-04-06   | D        | management write endpoints     | accept      | MVP local SQLite + low traffic; acceptable for current scope                |

</threat_model>

<verification>
- `npm run test -- src/app/api/workshops/route.test.ts`
- `npm run build`
</verification>

<success_criteria>

- MGMT-01, MGMT-02, MGMT-03, MGMT-04 API contracts are complete.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-winery-and-workshop-management/04-winery-and-workshop-management-02-SUMMARY.md`
</output>
