---
phase: 01-foundation-and-local-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/styles/tokens.css
  - src/lib/db/prisma.ts
  - prisma/schema.prisma
autonomous: true
requirements:
  - DATA-01
  - DATA-04
  - DATA-05
must_haves:
  truths:
    - "App boots with modular boundaries for app, ui, domain, and data access"
    - "Database schema stores matching-ready wine features"
    - "Additive structure exists and is queryable"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "SQLite models for wineries, wines, additives"
    - path: "src/lib/db/prisma.ts"
      provides: "Singleton Prisma client for local runtime"
    - path: "src/styles/tokens.css"
      provides: "Global UI tokens from UI-SPEC"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/styles/tokens.css"
      via: "global stylesheet import"
      pattern: "import.*tokens.css"
    - from: "src/lib/db/prisma.ts"
      to: "prisma/schema.prisma"
      via: "generated Prisma client"
      pattern: "new PrismaClient"
---

<objective>
Bootstrap the application foundation and schema contracts for local SQLite MVP.

Purpose: Establish modular structure and durable data contracts before seed and runtime validation.
Output: Next.js baseline scaffold, design tokens baseline, Prisma schema + client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-local-data/01-CONTEXT.md
@.planning/phases/01-foundation-and-local-data/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create modular app shell and tokenized UI baseline</name>
  <files>package.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/styles/tokens.css</files>
  <read_first>.planning/phases/01-foundation-and-local-data/01-UI-SPEC.md, .planning/PROJECT.md</read_first>
  <action>Initialize a Next.js App Router structure that enforces D-01 and D-03: keep app entrypoints in src/app, shared styling tokens in src/styles/tokens.css, and use the exact token variables from UI-SPEC (`--vm-bg`, `--vm-surface`, `--vm-ink`, `--vm-muted`, `--vm-accent`, `--vm-accent-2`, `--vm-border`). In src/app/layout.tsx import tokens.css and apply the font stack Space Grotesk/Manrope. In src/app/page.tsx render a simple foundation screen with an AppShell-like container and SectionTitle-like heading so phase success criterion "app boots" is testable. Keep implementation minimal KISS per D-07.</action>
  <verify>
    <automated>npm run lint || true && rg "--vm-accent|Space Grotesk|Manrope" src/styles/tokens.css src/app/layout.tsx && test -f src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/tokens.css` contains all seven required CSS variables.
    - `src/app/layout.tsx` imports `src/styles/tokens.css`.
    - `src/app/page.tsx` renders a non-empty foundation view.
  </acceptance_criteria>
  <done>App shell baseline exists and aligns with UI contract without implementing phase 2+ flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Prisma schema for wineries, wines, and additives</name>
  <files>prisma/schema.prisma, src/lib/db/prisma.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-foundation-and-local-data/01-CONTEXT.md</read_first>
  <behavior>
    - Model supports 1 winery to many wines.
    - Wine includes color, alcohol, vintage, body, aroma tags, price range.
    - Additive table exists with additive name and additive description.
  </behavior>
  <action>Implement SQLite datasource in prisma/schema.prisma per D-02 with models `Winery`, `Wine`, and `Additive`. Define relations so wines belong to one winery and optionally link to many additives through a join table (`WineAdditive`) to avoid future migration churn. Use concrete fields: Winery(name, slug, lat, lng, category), Wine(name, slug, color, alcoholPercent, vintage, bodyScore, aromaTags, priceRangeMin, priceRangeMax, wineryId), Additive(name, description). Create src/lib/db/prisma.ts singleton exporting `prisma` via `new PrismaClient()` for app/runtime usage.</action>
  <verify>
    <automated>npx prisma format && npx prisma validate && rg "model Winery|model Wine|model Additive" prisma/schema.prisma && test -f src/lib/db/prisma.ts</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` validates successfully.
    - Schema contains all three models plus join model for wine/additive relation.
    - `src/lib/db/prisma.ts` exports a Prisma singleton instance.
  </acceptance_criteria>
  <done>Data contract satisfies DATA-04 and DATA-05 and is ready for local push/seed.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push local Prisma schema before downstream verification</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>Run Prisma schema push command immediately after schema edits and before any further phase verification to avoid false-positive checks that rely only on generated types. Use `npx prisma db push` and if destructive prompt appears in non-interactive CI context, rerun with `npx prisma db push --accept-data-loss`.</action>
  <verify>
    <automated>npx prisma db push && npx prisma migrate status || true</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exits successfully.
    - Local SQLite file is created under configured datasource path.
  </acceptance_criteria>
  <done>Live local DB schema matches Prisma schema and phase can proceed to seed safely.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                         |
| ----------------------------------------- | ------------------------------------------------------------------- |
| local app -> local database               | schema and seed data cross from app runtime into persistent storage |
| future API input -> persistence contracts | untrusted payloads will later map into these models                 |

## STRIDE Threat Register

| Threat ID | Category | Component              | Disposition | Mitigation Plan                                                                  |
| --------- | -------- | ---------------------- | ----------- | -------------------------------------------------------------------------------- |
| T-01-01   | T        | prisma/schema.prisma   | mitigate    | define strict typed columns and relation keys to prevent silent shape drift      |
| T-01-02   | D        | src/lib/db/prisma.ts   | accept      | local MVP single-node scope; no external DB access in phase 1                    |
| T-01-03   | E        | future write endpoints | mitigate    | keep Prisma client centralized for upcoming validation middleware and zod checks |

</threat_model>

<verification>
- `npx prisma validate` passes
- `npx prisma db push` completes successfully
- Foundation page renders using tokenized styles
</verification>

<success_criteria>

- DATA-01 contract started with executable local schema workflow.
- DATA-04 fully covered by schema fields for matching/filter metadata.
- DATA-05 fully covered by additive model structure.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-local-data/01-foundation-and-local-data-01-SUMMARY.md`
</output>
