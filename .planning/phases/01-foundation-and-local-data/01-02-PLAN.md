---
phase: 01-foundation-and-local-data
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - prisma/seed.ts
  - package.json
  - src/lib/data/repositories/wineryRepository.ts
  - src/lib/data/repositories/wineRepository.ts
  - src/lib/data/repositories/additiveRepository.ts
  - scripts/verify-phase1-data.sh
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "Local DB can be initialized and seeded with target wineries"
    - "Seed data contains max 3 wines per winery"
    - "Repository layer separates persistence responsibility from app/UI"
  artifacts:
    - path: "prisma/seed.ts"
      provides: "deterministic seed dataset for 5 wineries and demo wines"
    - path: "src/lib/data/repositories/wineryRepository.ts"
      provides: "winery-specific persistence access"
    - path: "src/lib/data/repositories/wineRepository.ts"
      provides: "wine catalog query boundary"
    - path: "scripts/verify-phase1-data.sh"
      provides: "automated dataset validation checks"
  key_links:
    - from: "prisma/seed.ts"
      to: "prisma/schema.prisma"
      via: "prisma client createMany/upsert"
      pattern: "prisma\\.(winery|wine|additive)"
    - from: "package.json"
      to: "prisma/seed.ts"
      via: "seed script command"
      pattern: "prisma db seed"
---

<objective>
Populate local data foundation and repository boundaries for phase-1 functional readiness.

Purpose: Deliver deterministic local catalog setup so later matching/discovery phases can build on stable data.
Output: seed scripts, repository modules, validation shell script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-local-data/01-CONTEXT.md
@.planning/phases/01-foundation-and-local-data/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed the 5 requested wineries and capped wine catalog</name>
  <files>prisma/seed.ts, package.json</files>
  <read_first>prisma/schema.prisma, .planning/phases/01-foundation-and-local-data/01-CONTEXT.md</read_first>
  <behavior>
    - Exactly 5 target wineries exist after seed.
    - Each winery has at most 3 wines.
    - Seed can be re-run idempotently without duplicate rows.
  </behavior>
  <action>Create `prisma/seed.ts` using deterministic upsert logic for D-04 wineries with real coordinates: Pasqua Wine, Farina, Albino Armani, Ca' Rugate, Zyme. Insert up to 3 wines per winery (D-05), and include matching-ready fields from schema (color, alcoholPercent, vintage, bodyScore, aromaTags, priceRangeMin, priceRangeMax). If source descriptors are missing, fill values with sensible defaults and keep comments indicating estimated values. Configure `package.json` `prisma.seed` script to execute `tsx prisma/seed.ts` or equivalent TypeScript runtime command.</action>
  <verify>
    <automated>npx prisma db seed && node -e "const {PrismaClient}=require('@prisma/client');const p=new PrismaClient();(async()=>{const w=await p.winery.count();const bad=await p.winery.findMany({include:{wines:true}});const over=bad.filter(x=>x.wines.length>3).length;console.log('wineries',w,'overLimit',over);await p.$disconnect();process.exit((w===5&&over===0)?0:1)})()"</automated>
  </verify>
  <acceptance_criteria>
    - Seed command exits 0.
    - Winery count is exactly 5.
    - No winery has more than 3 wines.
  </acceptance_criteria>
  <done>DATA-02 and DATA-03 are concretely satisfied in local SQLite.</done>
</task>

<task type="auto">
  <name>Task 2: Add repository boundaries for wineries, wines, and additives</name>
  <files>src/lib/data/repositories/wineryRepository.ts, src/lib/data/repositories/wineRepository.ts, src/lib/data/repositories/additiveRepository.ts</files>
  <read_first>src/lib/db/prisma.ts, prisma/schema.prisma</read_first>
  <action>Create repository modules that encapsulate Prisma queries per aggregate (D-03): `wineryRepository` exports `listWineries()` and `getWineryBySlug(slug)`; `wineRepository` exports `listWines()` and `listWinesByWinery(wineryId)`; `additiveRepository` exports `listAdditives()` and `getAdditiveByName(name)`. Ensure functions return typed payloads from Prisma and avoid leaking raw query logic into app pages.</action>
  <verify>
    <automated>rg "export async function (listWineries|getWineryBySlug|listWines|listWinesByWinery|listAdditives|getAdditiveByName)" src/lib/data/repositories/*.ts</automated>
  </verify>
  <acceptance_criteria>
    - Three repository files exist under `src/lib/data/repositories`.
    - Each repository exports at least the listed functions.
    - No repository imports from app routes/pages.
  </acceptance_criteria>
  <done>Persistence responsibilities are modularized and reusable for future phases.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated phase-1 data validation script</name>
  <files>scripts/verify-phase1-data.sh</files>
  <read_first>prisma/seed.ts, prisma/schema.prisma</read_first>
  <action>Create executable shell script `scripts/verify-phase1-data.sh` that runs: prisma validate, prisma db push, prisma db seed, and SQL/query checks for required fields not null (`color`, `alcoholPercent`, `vintage`) and additive table accessibility. Script must fail non-zero on any broken assumption so phase verification can be automated in CI/local terminal.</action>
  <verify>
    <automated>bash scripts/verify-phase1-data.sh</automated>
  </verify>
  <acceptance_criteria>
    - Script is executable and exits 0 on healthy setup.
    - Script exits non-zero when winery count != 5 or wine cap is violated.
  </acceptance_criteria>
  <done>DATA-01 has an explicit repeatable verification path for local initialization.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                   |
| ---------------------------------- | ------------------------------------------------------------- |
| seed script -> database            | scripted writes populate persistent dataset                   |
| repository API -> future endpoints | data access layer becomes trust boundary for later user input |

## STRIDE Threat Register

| Threat ID | Category | Component                     | Disposition | Mitigation Plan                                                                           |
| --------- | -------- | ----------------------------- | ----------- | ----------------------------------------------------------------------------------------- |
| T-01-04   | T        | prisma/seed.ts                | mitigate    | enforce deterministic upsert keys (slug) to prevent duplicate or mutated identity records |
| T-01-05   | R        | scripts/verify-phase1-data.sh | mitigate    | include explicit failing checks and terminal output for auditability                      |
| T-01-06   | I        | repository layer              | accept      | local non-PII dataset only in phase 1; no sensitive fields modeled                        |

</threat_model>

<verification>
- `npx prisma db seed` produces deterministic expected counts.
- `bash scripts/verify-phase1-data.sh` passes.
- Repository exports are available for downstream app/API layers.
</verification>

<success_criteria>

- DATA-01 completed with repeatable setup and verification commands.
- DATA-02 completed with 5 real wineries stored locally.
- DATA-03 completed with max 3 wines per winery.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-local-data/01-foundation-and-local-data-02-SUMMARY.md`
</output>
