---
phase: 02-questionnaire-and-matching-core
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - src/lib/domain/matching/questionBank.ts
  - src/lib/domain/matching/surveyEngine.ts
  - src/app/api/matching/survey/route.ts
  - src/app/(public)/questionnaire/page.tsx
  - src/components/questionnaire/QuestionnaireFlow.tsx
  - src/components/questionnaire/QuestionStepCard.tsx
  - src/components/questionnaire/QuestionnaireProgress.tsx
autonomous: true
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-03
must_haves:
  truths:
    - "First user screen is questionnaire"
    - "Questionnaire remains non-technical and short"
    - "Survey answers generate persisted user vector"
  artifacts:
    - path: "src/lib/domain/matching/questionBank.ts"
      provides: "5-7 question definitions with hidden feature weights"
    - path: "src/lib/domain/matching/surveyEngine.ts"
      provides: "answer aggregation into clamped baseline vector"
    - path: "src/app/api/matching/survey/route.ts"
      provides: "survey submission and profile persistence endpoint"
  key_links:
    - from: "src/components/questionnaire/QuestionnaireFlow.tsx"
      to: "/api/matching/survey"
      via: "submit answers"
      pattern: "fetch\('/api/matching/survey'"
    - from: "src/lib/domain/matching/surveyEngine.ts"
      to: "src/lib/domain/matching/vectorMath.ts"
      via: "vector build"
      pattern: "buildSurveyVector"
---

<objective>
Implement questionnaire-first experience and survey-to-vector pipeline.

Purpose: Capture user preferences in a friendly way and convert them into stable matching input.
Output: question bank, survey engine, submission API, questionnaire UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-questionnaire-and-matching-core/02-CONTEXT.md
@.planning/phases/02-questionnaire-and-matching-core/02-UI-SPEC.md
@.planning/phases/02-questionnaire-and-matching-core/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build hidden-weight question bank and survey aggregation engine</name>
  <files>src/lib/domain/matching/questionBank.ts, src/lib/domain/matching/surveyEngine.ts</files>
  <read_first>/home/margy/Downloads/wine-matcher-algorithm.md, src/lib/domain/matching/features.ts, src/lib/domain/matching/vectorMath.ts</read_first>
  <behavior>
    - Exactly 5-7 non-technical questions.
    - Each option maps to 2-5 feature weights.
    - Every feature is touched by at least 2 questions.
  </behavior>
  <action>Create `questionBank.ts` with 6 questions in everyday language (coffee style, sparkling water, fruit notes, floral/scent mood, spice preference, intensity occasion). For each option, define hidden `weights` object keyed by the 12 features. Implement `surveyEngine.ts` to validate answer IDs, gather option contributions, call `buildSurveyVector`, and return baseline vector + per-feature coverage stats. Add explicit guard that missing feature contributions default to 0 before clamping.</action>
  <verify>
    <automated>npm run test -- src/lib/domain/matching/surveyEngine.test.ts || npx vitest run src/lib/domain/matching/surveyEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - question bank exports 6 questions.
    - each option includes hidden weight object.
    - coverage utility shows all 12 features touched by >=2 questions.
  </acceptance_criteria>
  <done>Survey definitions are algorithm-compliant and ready for API/UI use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add survey submit API and persist session/profile baseline</name>
  <files>src/app/api/matching/survey/route.ts</files>
  <read_first>src/lib/domain/matching/surveyEngine.ts, src/lib/data/repositories/matchingRepository.ts</read_first>
  <behavior>
    - valid payload creates survey session and taste profile.
    - invalid answers return 400.
    - response returns session id, baseline vector, and ranked wine preview.
  </behavior>
  <action>Implement POST `/api/matching/survey` accepting `{ answers: [{questionId, optionId}] }`. Use `surveyEngine` to build baseline vector, persist `SurveySession`, `SurveyAnswer`, and `UserTasteProfile` (baseline + current initialized equal to baseline) through `matchingRepository`. Load wine vectors and return top ranked wines via `rankWinesBySimilarity`. Keep JSON response deterministic and include `sessionId`, `vector`, `topMatches` fields.</action>
  <verify>
    <automated>npm run test -- src/app/api/matching/survey/route.test.ts || npx vitest run src/app/api/matching/survey/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - API route exists and handles POST.
    - bad payload returns 400 with error message.
    - success payload includes `sessionId` and non-empty `topMatches`.
  </acceptance_criteria>
  <done>MATCH-03 persistence flow is functional through one API entrypoint.</done>
</task>

<task type="auto">
  <name>Task 3: Build questionnaire-first UI flow and submission wiring</name>
  <files>src/app/(public)/questionnaire/page.tsx, src/components/questionnaire/QuestionnaireFlow.tsx, src/components/questionnaire/QuestionStepCard.tsx, src/components/questionnaire/QuestionnaireProgress.tsx</files>
  <read_first>src/app/page.tsx, .planning/phases/02-questionnaire-and-matching-core/02-UI-SPEC.md</read_first>
  <action>Create `/questionnaire` page and make it the default first screen path for new sessions by redirecting `/` to `/questionnaire` when no survey session id is present. Implement one-question-per-step flow with progress indicator, option selection, and submit action wired to `/api/matching/survey`. Keep labels non-technical and mobile-first per UI-SPEC. After submit, render immediate top-match preview cards with like/dislike controls placeholder that call next plan endpoint.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/questionnaire` route renders the 6-question flow.
    - `/` sends new users to questionnaire.
    - submit triggers API and displays initial ranked results.
  </acceptance_criteria>
  <done>MATCH-01 and MATCH-02 are satisfied with working questionnaire-first UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser answers -> API route | untrusted client payload enters matching system |
| API response -> UI result rendering | computed matches exposed to user-facing components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | S | survey session identity | mitigate | generate opaque session id server-side and never trust client-generated ids |
| T-02-05 | T | questionnaire answer payload | mitigate | validate question/option ids against in-memory question bank allowlist |
| T-02-06 | I | top match payload | accept | MVP does not expose sensitive data; only catalog wine metadata returned |
</threat_model>

<verification>
- Survey unit/API tests pass.
- Build passes with questionnaire route.
- Root-to-questionnaire redirect works for new session.
</verification>

<success_criteria>
- MATCH-01 complete.
- MATCH-02 complete.
- MATCH-03 user-vector creation and persistence complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-questionnaire-and-matching-core/02-questionnaire-and-matching-core-02-SUMMARY.md`
</output>
