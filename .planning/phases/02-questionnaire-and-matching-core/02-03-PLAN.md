---
phase: 02-questionnaire-and-matching-core
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02-PLAN.md
files_modified:
  - src/app/api/matching/feedback/route.ts
  - src/lib/domain/matching/feedbackEngine.ts
  - src/lib/domain/matching/feedbackEngine.test.ts
  - src/components/matching/MatchResultsList.tsx
  - src/components/matching/WineFeedbackToggle.tsx
  - src/app/(public)/wine/[slug]/page.tsx
autonomous: true
requirements:
  - MATCH-04
  - MATCH-05
must_haves:
  truths:
    - "User can submit like/dislike from wine detail context"
    - "Feedback updates profile via baseline + learning rate rule"
    - "Ranking recalculates after every feedback event"
  artifacts:
    - path: "src/lib/domain/matching/feedbackEngine.ts"
      provides: "baseline-rebased feedback vector update"
    - path: "src/app/api/matching/feedback/route.ts"
      provides: "feedback write + rerank endpoint"
    - path: "src/components/matching/WineFeedbackToggle.tsx"
      provides: "UI control for like/dislike interaction"
  key_links:
    - from: "src/components/matching/WineFeedbackToggle.tsx"
      to: "/api/matching/feedback"
      via: "feedback submission"
      pattern: "fetch\('/api/matching/feedback'"
    - from: "src/app/api/matching/feedback/route.ts"
      to: "src/lib/domain/matching/feedbackEngine.ts"
      via: "profile update"
      pattern: "applyFeedbackFromBaseline"
---

<objective>
Complete feedback loop and reranking behavior required by the algorithm.

Purpose: Make recommendations adaptive while keeping stability through baseline rebasing.
Output: feedback engine, feedback API, UI controls, detail-page action wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-questionnaire-and-matching-core/02-CONTEXT.md
@.planning/phases/02-questionnaire-and-matching-core/02-02-PLAN.md
@/home/margy/Downloads/wine-matcher-algorithm.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement baseline-rebased feedback engine</name>
  <files>src/lib/domain/matching/feedbackEngine.ts, src/lib/domain/matching/feedbackEngine.test.ts</files>
  <read_first>src/lib/domain/matching/vectorMath.ts, /home/margy/Downloads/wine-matcher-algorithm.md</read_first>
  <behavior>
    - like moves vector toward wine vector.
    - dislike moves vector away from wine vector.
    - computation starts from baseline vector each run.
  </behavior>
  <action>Create `feedbackEngine.ts` with function `recomputeProfileFromEvents({ baselineVector, wineVectorsById, events, learningRate })` returning updated current vector and ranked results. Use +direction for LIKE and -direction for DISLIKE per feature, clamp [0,1], and re-run ranking at end. In tests assert no cascade amplification by comparing two recomputations with same event set returning same vector.</action>
  <verify>
    <automated>npm run test -- src/lib/domain/matching/feedbackEngine.test.ts || npx vitest run src/lib/domain/matching/feedbackEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests cover like, dislike, and non-cascading recompute behavior.
    - learningRate default is 0.25 when profile value absent.
  </acceptance_criteria>
  <done>Algorithm feedback rule is implemented exactly as specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add feedback API endpoint and persist event history</name>
  <files>src/app/api/matching/feedback/route.ts</files>
  <read_first>src/lib/domain/matching/feedbackEngine.ts, src/lib/data/repositories/matchingRepository.ts</read_first>
  <behavior>
    - endpoint accepts like/dislike event with session and wine ids.
    - event is stored, profile current vector updated, ranking returned.
    - invalid session/wine returns 404 or 400.
  </behavior>
  <action>Implement POST `/api/matching/feedback` with payload `{ sessionId, wineId, feedback }`. Validate `feedback` in enum LIKE/DISLIKE. Persist event in `WineFeedbackEvent`, load baseline vector and all prior events for session, recompute current profile with `feedbackEngine`, persist updated current vector in `UserTasteProfile`, and return `updatedVector` + `topMatches`. Ensure response ordering uses `rankWinesBySimilarity` over current vector.
</action>
  <verify>
    <automated>npm run test -- src/app/api/matching/feedback/route.test.ts || npx vitest run src/app/api/matching/feedback/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - POST route exists and validates payload.
    - successful response includes updated ranked list.
    - persisted event count increases by one per request.
  </acceptance_criteria>
  <done>MATCH-05 runtime behavior is available through API.
</done>
</task>

<task type="auto">
  <name>Task 3: Wire like/dislike interaction into results and wine detail page</name>
  <files>src/components/matching/MatchResultsList.tsx, src/components/matching/WineFeedbackToggle.tsx, src/app/(public)/wine/[slug]/page.tsx</files>
  <read_first>src/components/questionnaire/QuestionnaireFlow.tsx, src/app/(public)/questionnaire/page.tsx</read_first>
  <action>Create reusable `WineFeedbackToggle` component with Like/Dislike controls calling `/api/matching/feedback`. Integrate it in `MatchResultsList` and in `src/app/(public)/wine/[slug]/page.tsx` so user can send feedback from wine detail context (MATCH-04). After success, refresh visible ranking section in results list.
</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - wine detail page exposes like/dislike controls.
    - triggering control calls feedback API and updates list.
    - build passes without type errors.
  </acceptance_criteria>
  <done>MATCH-04 and MATCH-05 are complete at user interaction level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feedback request -> profile state | user action mutates recommendation profile |
| detail page controls -> API | browser-triggered events influence ranking data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | feedback payload | mitigate | enforce enum validation and session/wine ownership checks before persistence |
| T-02-08 | R | profile recompute | mitigate | keep immutable event history and deterministic recompute from baseline |
| T-02-09 | E | wine detail action | accept | anonymous MVP scope; no privilege tiers in phase 2 |
</threat_model>

<verification>
- Feedback engine tests pass.
- Feedback API tests pass.
- Build passes with wine detail feedback controls.
</verification>

<success_criteria>
- MATCH-04 complete.
- MATCH-05 complete with reranking after feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/02-questionnaire-and-matching-core/02-questionnaire-and-matching-core-03-SUMMARY.md`
</output>
