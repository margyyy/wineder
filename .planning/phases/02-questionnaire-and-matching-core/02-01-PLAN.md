---
phase: 02-questionnaire-and-matching-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - src/lib/domain/matching/features.ts
  - src/lib/domain/matching/vectorMath.ts
  - src/lib/domain/matching/vectorMath.test.ts
  - src/lib/data/repositories/matchingRepository.ts
autonomous: true
requirements:
  - MATCH-03
  - MATCH-05
must_haves:
  truths:
    - "Wine and user vectors use identical 12-feature contract"
    - "Matching score is computed with cosine similarity in [0,1]"
    - "Feedback rebases from original survey vector"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Persistent models for survey session, vector profile, and feedback"
    - path: "src/lib/domain/matching/features.ts"
      provides: "Single source of truth for feature keys"
    - path: "src/lib/domain/matching/vectorMath.ts"
      provides: "Cosine similarity and vector update functions"
  key_links:
    - from: "src/lib/domain/matching/features.ts"
      to: "prisma/schema.prisma"
      via: "shared feature keys"
      pattern: "dolcezza|acidita|tannini|corpo"
    - from: "src/lib/domain/matching/vectorMath.ts"
      to: "src/lib/data/repositories/matchingRepository.ts"
      via: "ranking computation"
      pattern: "cosineSimilarity"
---

<objective>
Prepare phase-2 algorithm and data contracts by aligning DB structure with the Wine Matcher specification.

Purpose: Without this contract, questionnaire answers cannot be converted into stable vectors and deterministic ranking.
Output: schema extensions, matching constants, vector math primitives, matching repository.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-questionnaire-and-matching-core/02-CONTEXT.md
@.planning/phases/02-questionnaire-and-matching-core/02-RESEARCH.md
@/home/margy/Downloads/wine-matcher-algorithm.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DB schema for survey vectors and feedback loop</name>
  <files>prisma/schema.prisma, prisma/seed.ts, src/lib/data/repositories/matchingRepository.ts</files>
  <read_first>prisma/schema.prisma, prisma/seed.ts, /home/margy/Downloads/wine-matcher-algorithm.md</read_first>
  <behavior>
    - Feature space for wine and user profile is identical across 12 dimensions.
    - Survey session persists baseline vector and active vector.
    - Feedback events are stored with like/dislike and learning rate metadata.
  </behavior>
  <action>Update `prisma/schema.prisma` per D-04 and D-07 by adding explicit normalized fields (0..1) on `Wine` for each feature: `dolcezza`, `acidita`, `tannini`, `corpo`, `alcol`, `effervescenza`, `fruttato`, `floreale`, `speziato`, `terroso`, `legnoso`, `minerale`. Add models `SurveySession`, `SurveyAnswer`, `UserTasteProfile`, `WineFeedbackEvent`. `UserTasteProfile` must store both `baseline*` and `current*` values for all 12 features plus `learningRate`. `WineFeedbackEvent` must reference `surveySessionId` and `wineId` with `feedback` enum (`LIKE`,`DISLIKE`) and createdAt timestamp. Update `prisma/seed.ts` to populate all new wine feature fields with normalized values. Create `src/lib/data/repositories/matchingRepository.ts` exposing persistence methods for survey session creation, profile save/load, and feedback event write/read.</action>
  <verify>
    <automated>npx prisma format && npx prisma validate && npx prisma db push && npx prisma db seed</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains all 12 feature fields on `Wine`.
    - schema contains `SurveySession`, `SurveyAnswer`, `UserTasteProfile`, `WineFeedbackEvent`.
    - `prisma db push` and `prisma db seed` both pass.
  </acceptance_criteria>
  <done>DB structure supports exact algorithm flow from survey baseline to feedback reranking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared feature contract and cosine vector math</name>
  <files>src/lib/domain/matching/features.ts, src/lib/domain/matching/vectorMath.ts, src/lib/domain/matching/vectorMath.test.ts</files>
  <read_first>/home/margy/Downloads/wine-matcher-algorithm.md, src/lib/data/repositories/matchingRepository.ts</read_first>
  <behavior>
    - cosineSimilarity returns value in [0,1].
    - survey contributions are averaged per feature and clamped to [0,1].
    - feedback update recalculates current vector from baseline + event set using learning rate.
  </behavior>
  <action>Create `features.ts` exporting ordered tuple of 12 keys and utility types. Implement in `vectorMath.ts`: `buildSurveyVector(contributions)`, `cosineSimilarity(userVector,wineVector)`, `rankWinesBySimilarity(userVector,wines)`, `applyFeedbackFromBaseline(baselineVector, feedbackEvents, wineVectors, learningRate=0.25)`. Clamp all feature results to `[0,1]`. Write `vectorMath.test.ts` covering: (1) cosine identical vectors -> 1, orthogonal -> 0, (2) negative contributions clamped to zero in survey vector, (3) repeated feedback on same wine does not cascade because function recomputes from baseline.
</action>
  <verify>
    <automated>npm run test -- src/lib/domain/matching/vectorMath.test.ts || npx vitest run src/lib/domain/matching/vectorMath.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test file exists and includes at least three behavior tests listed above.
    - ranking helper sorts descending by similarity score.
    - feedback function requires baseline vector input explicitly.
  </acceptance_criteria>
  <done>Core deterministic matching algorithm is codified and test-backed.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema after phase-2 model changes</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>Run `npx prisma db push` after all schema edits and before downstream API/UI tasks. If a destructive warning appears in non-interactive mode, rerun with `npx prisma db push --accept-data-loss`.</action>
  <verify>
    <automated>npx prisma db push && npx prisma migrate status || true</automated>
  </verify>
  <acceptance_criteria>
    - DB sync command exits 0.
    - Generated client includes new phase-2 models.
  </acceptance_criteria>
  <done>Live local DB matches updated schema for questionnaire and feedback work.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                      |
| ------------------------------------------- | ------------------------------------------------ |
| questionnaire payload -> vector computation | untrusted user responses influence ranking state |
| feedback events -> persistent profile       | user feedback mutates recommendation profile     |

## STRIDE Threat Register

| Threat ID | Category | Component                 | Disposition | Mitigation Plan                                                                 |
| --------- | -------- | ------------------------- | ----------- | ------------------------------------------------------------------------------- |
| T-02-01   | T        | survey answer persistence | mitigate    | enforce feature-key allowlist via shared constants and typed DTO mapping        |
| T-02-02   | R        | feedback event history    | mitigate    | persist immutable feedback events with timestamp for reproducible recomputation |
| T-02-03   | D        | ranking service           | accept      | local MVP dataset is small; deterministic O(n) ranking acceptable               |

</threat_model>

<verification>
- Prisma schema validates and pushes with new models.
- Vector math tests pass.
- Seed populates new wine feature fields.
</verification>

<success_criteria>

- MATCH-03 technical foundation is complete.
- MATCH-05 algorithm prerequisites are complete.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-questionnaire-and-matching-core/02-questionnaire-and-matching-core-01-SUMMARY.md`
</output>
