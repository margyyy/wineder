---
phase: 03-discovery-ux-and-wine-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/domain/discovery/haversine.ts
  - src/lib/domain/discovery/filterEngine.ts
  - src/lib/domain/discovery/filterEngine.test.ts
  - src/lib/data/repositories/discoveryRepository.ts
  - src/app/api/discovery/route.ts
  - src/app/api/discovery/validators.ts
  - src/app/api/discovery/route.test.ts
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - DISC-06
  - DISC-07
must_haves:
  truths:
    - "Nearby matched wines are queryable from one endpoint"
    - "Distance, winery, vintage, alcohol, color, and price filters can run together"
    - "Filter output is deterministic for same session + filter input"
  artifacts:
    - path: "src/app/api/discovery/route.ts"
      provides: "Filtered discovery response with ranked wines"
    - path: "src/lib/domain/discovery/filterEngine.ts"
      provides: "Composable in-memory filter predicates"
    - path: "src/lib/domain/discovery/haversine.ts"
      provides: "Distance computation in kilometers"
  key_links:
    - from: "src/app/api/discovery/route.ts"
      to: "src/lib/data/repositories/discoveryRepository.ts"
      via: "query execution"
      pattern: "listDiscoverableWines"
    - from: "src/lib/data/repositories/discoveryRepository.ts"
      to: "src/lib/domain/discovery/filterEngine.ts"
      via: "filter composition"
      pattern: "applyDiscoveryFilters"
---

<objective>
Create the discovery data and API foundation that powers nearby results and multi-filter behavior.

Purpose: Phase 3 UI cannot be reliable without a deterministic filtered data contract.
Output: discovery domain utilities, repository query layer, discovery route with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/lib/data/repositories/wineRepository.ts
@src/lib/data/repositories/matchingRepository.ts
@src/lib/domain/matching/vectorMath.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement discovery distance and filter domain primitives</name>
  <files>src/lib/domain/discovery/haversine.ts, src/lib/domain/discovery/filterEngine.ts, src/lib/domain/discovery/filterEngine.test.ts</files>
  <behavior>
    - Haversine distance returns stable kilometer values for winery and user coordinates.
    - Filter engine applies all active filters as AND conditions.
    - Empty filter values do not remove rows.
  </behavior>
  <action>Create `haversine.ts` with `distanceKm(from,to)` and `filterEngine.ts` with `applyDiscoveryFilters(items, filters)` plus typed `DiscoveryFilters` covering `maxDistanceKm`, `wineryId`, `vintage`, `minAlcohol`, `maxAlcohol`, `color`, `priceMin`, `priceMax`. Add tests validating combined filters and no-filter pass-through behavior.</action>
  <verify>
    <automated>npm run test -- src/lib/domain/discovery/filterEngine.test.ts || npx vitest run src/lib/domain/discovery/filterEngine.test.ts</automated>
  </verify>
  <done>Deterministic distance and filter primitives are available for repository/API use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build discovery repository with match-aware query shape</name>
  <files>src/lib/data/repositories/discoveryRepository.ts</files>
  <action>Create `listDiscoverableWines({ sessionId, userLocation, filters, useMatchFilter })` that loads wines with winery relation, computes distance via `distanceKm`, optionally enriches with match score from current profile (`matchingRepository` + cosine ranking), and applies filter predicates from `filterEngine`. Return card-ready fields: ids/slugs, winery, color, alcohol, vintage, price range, distanceKm, score, imageUrl fallback field.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Repository returns a single normalized result shape for UI and API consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose GET /api/discovery with validated query params</name>
  <files>src/app/api/discovery/route.ts, src/app/api/discovery/validators.ts, src/app/api/discovery/route.test.ts</files>
  <behavior>
    - Invalid numeric filters return 400.
    - Valid requests return filtered list sorted by score when match filter is enabled.
    - Response includes distance and card metadata for UI.
  </behavior>
  <action>Create `validators.ts` to parse query params into `DiscoveryFilters` and `useMatchFilter` boolean. Implement GET route in `route.ts` reading session cookie and optional `lat/lng` (fallback to Verona center constants when absent), calling `listDiscoverableWines`, and returning JSON `{ filters, count, wines }`. Add route tests for invalid params and success response shape.</action>
  <verify>
    <automated>npm run test -- src/app/api/discovery/route.test.ts || npx vitest run src/app/api/discovery/route.test.ts</automated>
  </verify>
  <done>DISC-01..DISC-07 backend contract is complete and test-covered.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                     |
| ---------------------------------- | --------------------------------------------------------------- |
| query params -> discovery route    | untrusted filter input reaches server filtering logic           |
| session cookie -> match enrichment | anonymous session identifier selects personalized ranking state |

## STRIDE Threat Register

| Threat ID | Category | Component              | Disposition | Mitigation Plan                                                              |
| --------- | -------- | ---------------------- | ----------- | ---------------------------------------------------------------------------- |
| T-03-01   | T        | discovery query parser | mitigate    | validate and clamp numeric filters in `validators.ts` before repository call |
| T-03-02   | I        | discovery API response | accept      | response contains catalog metadata only; no PII in MVP                       |
| T-03-03   | D        | discovery endpoint     | mitigate    | bounded dataset + server-side filter composition with no unbounded joins     |

</threat_model>

<verification>
- `npm run test -- src/lib/domain/discovery/filterEngine.test.ts`
- `npm run test -- src/app/api/discovery/route.test.ts`
- `npm run build`
</verification>

<success_criteria>

- DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, DISC-06, DISC-07 are fully covered at API/domain layer.
- Discovery API returns card-ready rows with distance and score fields.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-ux-and-wine-detail/03-discovery-ux-and-wine-detail-01-SUMMARY.md`
</output>
