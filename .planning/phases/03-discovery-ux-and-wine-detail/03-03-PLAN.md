---
phase: 03-discovery-ux-and-wine-detail
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/components/maps/WineAvailabilityMap.tsx
  - src/lib/data/repositories/discoveryRepository.ts
  - src/app/(public)/wine/[slug]/page.tsx
autonomous: false
requirements:
  - WINE-02
  - WINE-03
must_haves:
  truths:
    - "Wine detail page shows where the wine can be found"
    - "Map includes seeded winery coordinates as visible points"
    - "Detail remains usable on mobile and desktop"
  artifacts:
    - path: "src/components/maps/WineAvailabilityMap.tsx"
      provides: "Leaflet map rendering marker list"
    - path: "src/app/(public)/wine/[slug]/page.tsx"
      provides: "Wine detail composition with map section"
  key_links:
    - from: "src/app/(public)/wine/[slug]/page.tsx"
      to: "src/components/maps/WineAvailabilityMap.tsx"
      via: "client map embedding"
      pattern: "WineAvailabilityMap"
    - from: "src/lib/data/repositories/discoveryRepository.ts"
      to: "src/app/(public)/wine/[slug]/page.tsx"
      via: "availability points query"
      pattern: "listWineAvailabilityPoints"
---

<objective>
Add map-enabled wine detail experience with real seeded locations.

Purpose: Complete Phase 3 detail/map requirements with concrete geolocation output.
Output: availability query helper, map component, detail page integration and human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@prisma/schema.prisma
@src/app/(public)/wine/[slug]/page.tsx
@src/lib/data/repositories/wineryRepository.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provide wine availability points from seeded winery coordinates</name>
  <files>src/lib/data/repositories/discoveryRepository.ts</files>
  <action>Add `listWineAvailabilityPoints(slug)` returning wine metadata plus all matching winery points (`id`, `name`, `lat`, `lng`, `category`). Keep output minimal and map-ready so detail page avoids ad-hoc joins.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Detail page has a dedicated repository method for map location data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Leaflet-based map component and embed in wine detail</name>
  <files>src/components/maps/WineAvailabilityMap.tsx, src/app/(public)/wine/[slug]/page.tsx</files>
  <action>Create `WineAvailabilityMap` as a client component (dynamic import-safe) that renders OpenStreetMap tiles and markers for each availability point. Update wine detail page to load point data, render the map section under wine features, and keep existing feedback controls. Add fallback text when no points exist.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>WINE-02 and WINE-03 are technically implemented with real coordinates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification for map marker correctness and responsive layout</name>
  <action>Run an explicit UI verification pass after automated build to confirm marker placement and mobile behavior on real rendering output.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Human confirms map markers and responsive layout are acceptable, or returns concrete issues for fixes.</done>
  <what-built>Wine detail map rendering and marker placement on seeded coordinates</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `/discover`.
    2. Open at least one wine detail page via card link.
    3. Confirm map is visible and markers align with expected winery names.
    4. Confirm mobile viewport keeps map and details usable without overflow.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server location data -> client map | coordinate data is rendered into third-party tile layer context |
| dynamic map rendering -> browser runtime | client map library executes in user browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | map marker payload | mitigate | render markers from server-typed numeric coordinates only |
| T-03-08 | I | map tile provider requests | accept | OpenStreetMap tile access is expected and carries no sensitive payload |
| T-03-09 | D | client map bundle | mitigate | lazy-load map component on detail route only to reduce global JS cost |
</threat_model>

<verification>
- `npm run build`
- Checkpoint: human verifies marker correctness and responsive behavior.
</verification>

<success_criteria>
- WINE-02 and WINE-03 are complete and user-verifiable.
- Detail page exposes map-based "where to find" information.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-ux-and-wine-detail/03-discovery-ux-and-wine-detail-03-SUMMARY.md`
</output>
