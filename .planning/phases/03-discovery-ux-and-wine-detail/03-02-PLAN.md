---
phase: 03-discovery-ux-and-wine-detail
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/app/page.tsx
  - src/app/(public)/discover/page.tsx
  - src/components/discovery/DiscoveryFiltersPanel.tsx
  - src/components/discovery/DiscoveryResultsGrid.tsx
  - src/components/discovery/WineResultCard.tsx
  - src/components/questionnaire/QuestionnaireFlow.tsx
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - DISC-06
  - DISC-07
  - DISC-08
  - WINE-01
must_haves:
  truths:
    - "Users see nearby matched wines after completing questionnaire"
    - "Users can combine filters and immediately refresh results"
    - "Users can disable match filtering and browse all wines in area"
    - "Wine cards show image, name, and core DB features"
  artifacts:
    - path: "src/app/(public)/discover/page.tsx"
      provides: "Discovery page shell and data loading state"
    - path: "src/components/discovery/DiscoveryFiltersPanel.tsx"
      provides: "Interactive controls for all DISC filters"
    - path: "src/components/discovery/WineResultCard.tsx"
      provides: "Wine card UI with image fallback and metadata"
  key_links:
    - from: "src/components/discovery/DiscoveryFiltersPanel.tsx"
      to: "/api/discovery"
      via: "query-string updates"
      pattern: "maxDistanceKm|wineryId|vintage|color"
    - from: "src/components/questionnaire/QuestionnaireFlow.tsx"
      to: "/discover"
      via: "post-submit navigation"
      pattern: "router.push\('/discover'\)"
---

<objective>
Deliver the discovery browsing UX with filter controls, match toggle, and card rendering.

Purpose: This is the user-visible core of phase 3 discovery requirements.
Output: discover route, filters panel, results grid, card component, onboarding redirect wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-discovery-ux-and-wine-detail/03-01-PLAN.md
@src/components/matching/MatchResultsList.tsx
@src/components/questionnaire/QuestionnaireFlow.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create discovery route and client data-fetch loop</name>
  <files>src/app/(public)/discover/page.tsx, src/components/discovery/DiscoveryResultsGrid.tsx</files>
  <action>Create `/discover` page that reads current session cookie state and renders a client results module. Build `DiscoveryResultsGrid` to fetch from `/api/discovery`, handle loading/error/empty states, and render sorted results with pagination-safe keys. Keep request state in URL query params so refresh preserves filter state.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Discover page loads and displays nearby matched wines using phase-3 API.</done>
</task>

<task type="auto">
  <name>Task 2: Implement complete filters panel plus match-toggle behavior</name>
  <files>src/components/discovery/DiscoveryFiltersPanel.tsx, src/components/discovery/DiscoveryResultsGrid.tsx</files>
  <action>Implement controls for all DISC filters: max distance slider, winery selector, vintage selector, alcohol range, color toggle, price range, and `useMatchFilter` switch for DISC-08. Wire controls to query params and refetch results on changes with debounce for slider/range inputs.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>DISC-02..DISC-08 controls work together and can be toggled without page crashes.</done>
</task>

<task type="auto">
  <name>Task 3: Build card component and questionnaire-to-discover navigation</name>
  <files>src/components/discovery/WineResultCard.tsx, src/components/discovery/DiscoveryResultsGrid.tsx, src/components/questionnaire/QuestionnaireFlow.tsx, src/app/page.tsx</files>
  <action>Create `WineResultCard` to show image with fallback asset, wine name, winery name, color, alcohol, vintage, price range, distance, and match score when enabled. Include link to `/wine/[slug]`. Update `QuestionnaireFlow` to navigate to `/discover` after successful survey submit. Update `src/app/page.tsx` so existing sessions are redirected to `/discover` and new sessions to `/questionnaire`.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>WINE-01 and DISC-01 entry flow are complete in UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser controls -> discovery API | untrusted client filter values are sent to server |
| card link navigation -> detail route | client route params influence detail query target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | filter controls serialization | mitigate | serialize and validate only known query keys in results grid before fetch |
| T-03-05 | S | session-dependent discover entry | mitigate | server route derives session from cookie, not client-injected identifier |
| T-03-06 | D | aggressive refetch on input | mitigate | debounce slider/range updates and cancel stale requests |
</threat_model>

<verification>
- `npm run build`
- Manual UAT: complete questionnaire, land on `/discover`, apply all filters, toggle match mode, open a wine detail page.
</verification>

<success_criteria>
- DISC-01..DISC-08 and WINE-01 are complete at UI level.
- Root redirect behavior matches onboarding state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-ux-and-wine-detail/03-discovery-ux-and-wine-detail-02-SUMMARY.md`
</output>
